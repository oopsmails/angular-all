export interface SampleArrayItem {
    id: number;
    name: string;
    description?: string;
    price?: number;
    imageUrl?: string;
    quantity?: number;
}

export interface OptionItem {
    optionId: number;
    optionTitle: string;
}
